"""Experimental backbone-norm ablation code.

This directory is intentionally isolated from the main `dinoct` package so it can
be removed after the ablation study without touching the core training path.
"""
